<div id="container" role="main">
	<input type="hidden" id="categoryId" value="{{categoryid}}"/>
	<input type="hidden" id="categoryName" value="{{categoryname}}"/>
	{% if categoryid is empty %}
	<p>Select a category to browse the stories within</p>
	<table id="categoryIndex" class="hover">
		<thead>
			<tr>
				<th>Name</th>
				<th>Stories</th>
			</tr>
		</thead>
		<tbody>
			{% for category in categories.categories %}
			{% if category.storiesCount > 0 %}
			<tr data-id="{{category.id}}" data-name="{{category.name}}">
				<td>{{category.nameReadable}}</td>
				<td>{{category.storiesCount}}</td>
			</tr>
			{% endif %}
			{% endfor %}
		</tbody>
	</table>
	{% else %}
	<p>Select a story to view it</p>
	<table id="storyIndex" class="hover" role="main">
		<thead>
			<tr>
				<th>ID</th>
				<th>Story</th>
			</tr>
		</thead>
		<tbody>
		</tbody>
	</table>
	{% endif %}
</div>